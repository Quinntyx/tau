# GUI Picker/Command Contract
Untracked; never commit. Full contract: `/home/henry/Documents/src/tau/main/ROADMAP.md`, M10/M13 and Q&A r5/r8/r15.

Scope: replace dead picker helpers with centered rendered GPUI model and agent modals. Model picker has search, provider labels/groups, favorites, recents, selection persistence; agent picker has search, cycle membership, server-backed selection. Implement `/agent <name>`, `/agents`, `/model`, command argument autocomplete, keyboard/mouse navigation, Escape/Enter, Tab agent cycling, and GUI buttons. Commands dispatch typed APIs/actions, never presentation parsing. Add component/action tests.
